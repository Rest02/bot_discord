services:
  app:
    command: sh -c "npx prisma generate && npx nest start --watch"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - LOG_LEVEL=debug
